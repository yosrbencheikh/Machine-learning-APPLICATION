# -*- coding: utf-8 -*-
""" from sqlalchemy import SQLAlchemy

db = SQLAlchemy()

def create_app(name='apg'):
    app = Flask(name,  template_folder="templates")
    app.config.from_object(config)
    config.init_app(app)
    excel.init_excel(app)

    bootstrap.init_app(app)
    db.init_app(app)
    login_manager.init_app(app)
    moment.init_app(app)
    mail.init_app(app)
    

    return app
 """